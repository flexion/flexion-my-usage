
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
*.gate.lock*

# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo

# Yarn (Berry, node-modules linker)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*

# Test / coverage
coverage/

# IP-leak guardrail: internal-term denylist stays local, never committed (myusage-6d9)
.guardrail/
